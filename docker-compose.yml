version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "628:628"
    environment:
      NODE_ENV: production
      PORT: 628
      DATABASE_URL: ${DATABASE_URL}
    restart: always
