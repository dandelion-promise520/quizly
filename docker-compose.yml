services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "628:628"
    volumes:
      - quiz-db-data:/app/data
    environment:
      - PORT=628
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/quiz.db
    restart: always

volumes:
  quiz-db-data:
